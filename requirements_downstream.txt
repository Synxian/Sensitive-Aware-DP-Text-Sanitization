# Minimal dependencies for downstream training scripts in downstream/.
# Install with: pip install -r requirements_downstream.txt

transformers>=4.38.0
datasets>=2.16.0
evaluate>=0.4.1
seqeval>=1.2.2
accelerate>=0.26.0
scikit-learn>=1.2.0
pandas>=2.0.0
pyarrow>=12.0.0
torch>=2.0.0
